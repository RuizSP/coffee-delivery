import styled from "styled-components";

export const BtnCart = styled.button`
    background: var(--purple-dark);
    border: none;
    width: 38px;
    height:38px;
    border-radius: 6px;
    cursor: pointer;
    &&:selectd

`